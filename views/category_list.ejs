<!DOCTYPE html>
<html lang="en">
	<%- include('templates/head') %>
	<body>
		<%- include('templates/navbar') %>
      <h1>All Categories</h1>
		<ul>
			<% for(let i = 0; i < category_list.length; i++) { %>
			<li><%= category_list[i].name %></li>
			<% } %>
		</ul>
	</body>
</html>
