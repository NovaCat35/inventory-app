<!DOCTYPE html>
<html lang="en">
	<%- include('templates/head') %>
	<body>
		<%- include('templates/navbar') %>
      <main>
			<h1><%= title %></h1>
					<form method="post" action="">
						<div class="form-group">
							<label for="category_name">Name *</label>
							<input type="text" name="category_name" id="category_name" placeholder="Home and Decor/Security" value="<%= (typeof category === "undefined" ? '' : category.name)%>" required>
						</div>
			
						<div class="form-group">
							<label for="category_description">Description *</label>
							<textarea name="category_description" id="category_description" rows="4" cols="50" minLength="10" maxLength="200" placeholder="Discover a collection of essential and stylish items designed to enhance your home's security while adding a touch of decor. Explore functional and aesthetic solutions for a safe and inviting living space." required><%= (typeof category === "undefined" ? '' : category.description) %></textarea>
						</div>
			
						<button type="submit">Submit</button>
					</form>
			<% if (typeof errors !== "undefined" && errors.length > 0) { %>
				<ul>
					<% errors.forEach(error => { %>
						<li class="error-msg"><%= error.msg %>!</li>
					<% }) %>
				</ul>
			<% } %>
			<img class="type-cat" id="food-cats" src="/images/type-cat-food.png" alt="cat image">
		</main>
	</body>
</html>
